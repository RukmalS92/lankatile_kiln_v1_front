import { InvDirectiveDirective } from './inv-directive.directive';

describe('InvDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new InvDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
